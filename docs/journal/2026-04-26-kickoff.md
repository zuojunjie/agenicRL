# 2026-04-26 · 项目启动会话

## 目标

确认本次对话的目的：通过一个真实场景的训练，把 Agentic RL 的技术栈学透。
强调互相启发的多轮规划，而非单方面灌输。

## 会话推进路径

### 1. 全景图分析

阅读 *The Landscape of Agentic RL for LLMs: A Survey* (TMLR 2026) 概览图，
17 篇必读论文，5 个能力维度（Planning / Tool Use / Memory / Self-Improvement / Reasoning）。

核心论点：
> LLM RL 优化「说什么」· Agentic RL 优化「怎么做」

### 2. 三个前置变量

| 变量 | 决定 |
|---|---|
| 算力预算 | 4×A100 |
| 场景偏好 | 搜索（Search-R1） |
| 学习深度 | 复现 + 论文递进 |

### 3. 学习路径：从 5 篇到 7 篇

最初提案：DeepSeek-R1 → DAPO → ToRL → GiGPO → ARPO（5 主干）

经过和全景图对比后调整：

- 🆕 **GSPO** 加入 Phase 2，与 DAPO 双读双比
- 🆕 **ToolRL** 作为 Phase 3 前置阅读（30min，不实现）
- ⚠️ **ReSum** 作为多跳长上下文备选弹药

### 4. Phase 0–5 依赖关系

详见 [架构/阶段依赖](../architecture/dependencies.md)。

关键认知：

- 硬依赖：P0 → P1 → P2，必须严格按序
- 软依赖：P3 ↔ P4，顺序可调
- 三个并行机会（早期被识别）：P0 训练时读 P1、P2 读 DAPO/GSPO 并行、P3/P4 阅读前置

### 5. 算力约束的重塑

**关键转折**：用户披露本地无 GPU，需租云。
工作流从"本地长跑"切到"本地 + 云端冲刺":

| 维度 | 决定 |
|---|---|
| 提供商 | AutoDL |
| 基座 | **Qwen2.5-3B-Instruct（全程不升 7B）** |
| 节奏 | 晚间常规型 |
| 训练原子性 | **每 run 一次跑完，不跨夜** |

用户明确反对 "跨夜续训" 的过度工程化设计。

### 6. Agent Teams 模式启用

会话中段，确认 Claude Code 的 Agent Teams 实验功能可用。
写入 `~/.claude/settings.json`：

- `env.CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`
- `teammateMode: auto`

加载工具：`TeamCreate` / `TeamDelete` / `SendMessage`。
**目前未实际开队**，等 Phase 0 启动后再启用。

### 7. 文档站点初始化

本文档站基于 MkDocs Material 搭建。所有决策、架构、阶段计划、会话日志统一记录。

## 当前阻塞 / 待回答

- [ ] 本地 Mac 是否有 ~100GB 富余磁盘？
- [ ] 文档站推送到 **GitHub** 还是 **Gitee**（影响国内访问速度）？
- [ ] 现在动手做 Phase -1 第一波，还是留到今晚？

## 下一步

进入 Phase -1 执行：环境搭建 → clone Search-R1 → 看 README 拍板决策 A（index 方案）。
